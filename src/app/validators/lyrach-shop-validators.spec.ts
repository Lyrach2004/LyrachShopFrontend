import { LyrachShopValidators } from './lyrach-shop-validators';

describe('LyrachShopValidators', () => {
  it('should create an instance', () => {
    expect(new LyrachShopValidators()).toBeTruthy();
  });
});
